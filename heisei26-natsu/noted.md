# 如何检测一个点是否在多边形内

### 多边形的描述：list[tuple[int, int]]，从 $x_0, x_1, ... x_n, x_0$ ，描绘$n$个线段

### 算法流程：
```
1. 找一个合适的方向 或者多个随机方向，引一条射线
2. 枚举所有顶点，如果顶点再线上，换一条线（这一步可以通过事先根据多边形几何性质找一个好角度得到）
2. 依次轮询所有的线段，计算交点个数，奇数在里面，偶数在外面
```